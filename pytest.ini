[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers
filterwarnings =
    error::SyntaxWarning
    error::DeprecationWarning:CUMBIA_CIR
    error::DeprecationWarning:CUMBIA_RECT
    ignore::DeprecationWarning
markers =
    slow: full engine runs (several seconds each)
